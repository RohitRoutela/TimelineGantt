export const VIEW_MODES = {
  DAY: 'day',
  WEEK: 'week',
  MONTH: 'month',
};

export const DEFAULT_ROW_WIDTH = 200;
export const DEFAULT_TASK_HEIGHT = 32;
export const DEFAULT_PIXELS_PER_DAY = {
  day: 40,
  week: 80,
  month: 120
};
